
<h1>Listing of patients</h1>
<div class="row">
  <% @patients.each do |patient| %>

  <div class="col-sm-6 col-md-3">
    <div class="thumbnail">

      <%= image_tag(patient.image , alt: "Picture of #{patient.name}") %>
    </div>
    <div class="caption">
     <h3> <%= patient.name %></h3>
     <p>
       <%= patient.age %> years old
     </p>
     <p>

       <%= link_to 'Show', patient , class: "btn btn-primary btn-sm", role: "button" %>
       <%= link_to 'Edit', edit_patient_path(patient), class: "btn btn-primary btn-sm", role: "button" %>
       <%= link_to 'Destroy', patient, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-primary btn-sm", role: "button" %>
     </p>
   </div>
 </div>
 <% end %>
</div>












<!-- <table class="table table-striped"> 
  <thead>
    <tr>
      <th>Name</th>
      <th>Nssnumber</th>
      <th>Age</th>
      <th>Address</th>
      <th>Image</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <%# @patients.each do |patient| %>
    <tr>
      <td><%#= patient.name %></td>
      <td><%#= patient.nssNumber %></td>
      <td><%#= patient.age %></td>
      <td><%#= patient.address %></td>
      <td><%#= patient.image %></td>
      <td><%#= link_to 'Show', patient %></td>
      <td><%#= link_to 'Edit', edit_patient_path(patient) %></td>
      <td><%#= link_to 'Destroy', patient, method: :delete, data: { confirm: 'Are you sure?' } %></td>
    </tr>
    <%# end %>
  </tbody>
</table> -->

<br>

<%= link_to 'New Patient', new_patient_path %>
